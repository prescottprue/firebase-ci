#!/usr/bin/env node
'use strict'

const program = require('commander')
require('autocmdr/lib/logger')(program)
require('../cmds')(program)

program.version('0.0.0')

program
  .on('*', function (name) {
    console.log('\'' + name + '\' is not a known command. See \'firebase-ci --help\':')
    program.outputHelp()
    process.exit(1)
  })

program
  .parse(process.argv)

if (program.args.length < 1) {
  console.log('No command specified. See \'firebase-ci --help\':')
  program.outputHelp()
  process.exit(1)
}
